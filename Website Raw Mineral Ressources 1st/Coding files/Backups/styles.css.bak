:root{
  --bg1: #f7e8ff; /* very pale purple */
  --accent1: #d7b3ff; /* pastel purple */
  --accent2: #ffd3e8; /* pastel pink */
  --text: #2b2340;
  --muted: #5b4f6a;
  --glass: rgba(255,255,255,0.6);
}
*{box-sizing:border-box}
html,body{height:100%}
body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial;color:var(--text);background:var(--bg1);-webkit-font-smoothing:antialiased}
.container{max-width:1000px;margin:0 auto;padding:2rem}

/* Header (hidden until scroll) */
header{position:fixed;left:0;right:0;top:0;z-index:50;transition:transform .35s ease,opacity .35s ease;padding:0.5rem 0}
header.hidden{opacity:0;transform:translateY(-15px);pointer-events:none}
header.visible{opacity:1;transform:none;pointer-events:auto}
header .container{display:flex;align-items:center;justify-content:space-between}
.logo{font-weight:700;color:var(--text);text-decoration:none}
nav a{margin-left:1.25rem;color:var(--muted);text-decoration:none}

#hamburger{display:none;background:transparent;border:0;font-size:1.25rem}
body:not(.home) #hamburger{display:block}

/* Hero */
.hero{position:relative;height:100vh;overflow:hidden}
.hero-img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}
.hero-overlay{position:relative;z-index:2;height:100%;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:2rem;color:white}
.hero h1{font-size:2.25rem;margin:0 0 0.5rem;color:#fff;text-shadow:0 6px 18px rgba(40,20,60,0.35)}
.subtitle{opacity:0.95;margin:0 0 1.6rem}
.scroll-hint{display:inline-block;padding:.5rem .9rem;border-radius:999px;background:rgba(255,255,255,0.15);color:#fff;text-decoration:none}

/* Page-specific: hide full hero on the traceability page so the page starts at commitments */
.trace .hero{display:none}
.trace .content{padding-top:2rem}

/* Keep the hamburger visible and interactive even when header is hidden (except homepage).
   We override pointer-events and position so the button remains accessible. */
header.hidden #hamburger{
  display:block;
  position:fixed;
  right:16px;
  top:12px;
  z-index:70;
  opacity:1;
  pointer-events:auto;
}

/* Content */
.content{padding:4rem 0;background:linear-gradient(180deg, rgba(255,255,255,0.6), rgba(255,255,255,0.9))}

/* Mobile menu */
.mobile-menu{position:fixed;top:60px;right:16px;background:var(--glass);backdrop-filter:blur(6px);padding:1rem;border-radius:12px;flex-direction:column;gap:0.5rem;z-index:60}
.mobile-menu a{color:var(--text);text-decoration:none}

/* Respect aria-hidden for the mobile menu: hide when true, show when false (all viewports).
   This lets the hamburger open an overlay/menu even on desktop if desired. */
.mobile-menu[aria-hidden="true"]{display:none}
.mobile-menu[aria-hidden="false"]{display:flex}

/* Responsive */
@media (max-width:800px){
  nav{display:none}
  #hamburger{display:block}
  .hero h1{font-size:1.6rem}
  .container{padding:1rem}
  .mobile-menu{display:flex}
}








































































































































































}  .team-grid{grid-template-columns:1fr}@media (max-width:600px){}  .team-grid{grid-template-columns:repeat(auto-fit, minmax(200px, 1fr))}@media (max-width:768px){.team-contact a:hover{text-decoration:underline}.team-contact a{color:var(--accent1);text-decoration:none}}  color:var(--text);  font-size:0.85rem;  margin-top:1rem;.team-contact{.team-qualifications li{margin:0.25rem 0}}  list-style:disc;  color:var(--muted);  font-size:0.85rem;  padding-left:1.5rem;  margin:0.5rem 0;.team-qualifications ul{}  font-size:0.9rem;  margin:0.5rem 0 0;.team-qualifications p{}  border-radius:6px;  background:rgba(215,179,255,0.08);  padding:1rem;  margin:1rem 0;.team-qualifications{}  font-style:italic;  font-size:0.9rem;  color:var(--muted);  margin:0.5rem 0;.team-role{}  font-size:1.15rem;  color:var(--accent1);  margin-top:0;.team-content h3{.team-content{padding:1.5rem}}  object-fit:cover;  height:100%;  width:100%;.team-image img{}  background:linear-gradient(135deg, var(--accent2), var(--accent1));  overflow:hidden;  height:200px;  width:100%;.team-image{}  box-shadow:0 8px 24px rgba(40,20,60,0.15);  transform:translateY(-4px);.team-card:hover{}  transition:transform 0.2s ease, box-shadow 0.2s ease;  box-shadow:0 4px 16px rgba(40,20,60,0.08);  overflow:hidden;  border-radius:12px;  background:rgba(255,255,255,0.5);.team-card{}  margin:2rem 0;  gap:2rem;  grid-template-columns:repeat(auto-fit, minmax(260px, 1fr));  display:grid;.team-grid{.team-intro{color:var(--muted);font-size:0.95rem;margin-bottom:2rem;text-align:center}.team-section{margin:3rem 0}/* Team Section */.cert-note{font-size:0.9rem;color:var(--muted);margin-top:1rem}.cert-link:hover{opacity:.95}.cert-link{display:inline-block;padding:.45rem .65rem;border-radius:8px;background:var(--accent1);color:#fff;text-decoration:none}.cert-status{font-size:0.95rem;color:var(--muted);margin:0 0 .6rem}.cert-card h3{margin:0 0 .4rem;color:var(--accent1)}.cert-card{background:rgba(255,255,255,0.6);padding:1rem;border-radius:10px;box-shadow:0 4px 14px rgba(40,20,60,0.06)}.cert-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:1.25rem;margin:1.5rem 0}.cert-intro{color:var(--muted);margin-bottom:1rem}.cert-section{padding:3rem 0;background:linear-gradient(180deg, rgba(255,255,255,0.98), rgba(255,255,255,0.98))}/* Certifications / Traceability documents */}  .products-grid{grid-template-columns:1fr}@media (max-width:600px){}  .products-grid{grid-template-columns:repeat(auto-fit, minmax(200px, 1fr))}  .about-grid{grid-template-columns:1fr;gap:1rem}@media (max-width:768px){/* Responsive: tablets and mobile */}  font-size:0.95rem;  line-height:1.5;  color:var(--muted);  margin-bottom:0;.product-content p{}  color:var(--accent1);  margin-top:0;.product-content h3{}  padding:1.5rem;.product-content{}  object-fit:cover;  height:100%;  width:100%;.product-image img{}  overflow:hidden;  height:200px;  width:100%;.product-image{}  box-shadow:0 8px 24px rgba(40,20,60,0.15);  transform:translateY(-4px);.product-card:hover{}  transition:transform 0.2s ease, box-shadow 0.2s ease;  box-shadow:0 4px 16px rgba(40,20,60,0.08);  overflow:hidden;  border-radius:12px;  background:rgba(255,255,255,0.5);.product-card{}  margin:2rem 0;  gap:2rem;  grid-template-columns:repeat(auto-fit, minmax(280px, 1fr));  display:grid;.products-grid{/* Products Grid */}  border-radius:6px;  padding:1rem;  background:rgba(255,255,255,0.02);  line-height:1.5;  color:var(--muted);  border-left:4px solid rgba(215,179,255,0.8);  padding-left:1rem;  margin:0;.about-grid blockquote{.about-grid .about-text h2{margin-top:0}}  box-shadow: 0 8px 30px rgba(40,20,60,0.12);  object-fit:cover;  border-radius:8px;  height:auto;  width:100%;.about-grid .about-image img{}  margin:2rem 0;  align-items:center;  gap:2rem;  grid-template-columns: 1fr 1fr;  display:grid;.about-grid{/* About two-column layout */a{color:var(--accent1)}n/* small accessibility + polish */